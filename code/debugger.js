// let inputs = [];
// let labels = ["Parameter 1", "Parameter 2", "Parameter 3"]; // Add more labels as needed

// function drawDebugMenu() {
//     let xOffset = width + 20; // Position inputs outside the canvas
//     let yOffset = 20; // Starting y position for inputs

//     for (let i = 0; i < labels.length; i++) {
//         // Create label
//         let label = createElement('label', labels[i]);
//         label.position(xOffset, yOffset + i * 30);

//         // Create input box
//         let input = createInput('');
//         input.position(xOffset + 100, yOffset + i * 30);
//         inputs.push(input);
//     }
// }

// // Function to get values from inputs
// function getDebugValues() {
//     let values = inputs.map(input => input.value());
//     console.log(values); // Print values to console for debugging
//     return values;
// }
